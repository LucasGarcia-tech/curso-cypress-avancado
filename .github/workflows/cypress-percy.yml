name: Cypress + Percy tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      # ⚠️ Esta etapa estava com erro antes. 'npx percy install' não é necessário com o Percy CLI.
      # Você pode REMOVER essa etapa ou substituí-la por um comentário.
      # Percy CLI não tem o comando `install`.
      # - name: Install Percy
      #   run: npx percy install

      - name: Run Cypress tes
